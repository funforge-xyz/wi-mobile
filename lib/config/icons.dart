import 'package:flutter/material.dart';
import 'package:wi/config/assets.dart';
import 'package:wi/widgets/custom_icon.dart';

class I {
  // Common
  static const back = Icons.arrow_back_ios;
  static const menu = Icons.menu;
  static final user = CustomIconData(A.icon('user'));
  static final camera = CustomIconData(A.icon('camera'));
  static final info = Icons.info_outline;
  static final phone = CustomIconData(A.icon('phone'));
  static final email = CustomIconData(A.icon('at-sign'));
  static final error = CustomIconData(A.icon('error'));
  static final connectionError = CustomIconData(A.icon('internet'));
  static final search = CustomIconData(A.icon('search'));
  static const overflowMenu = Icons.more_vert;
  static const settings = Icons.settings;
  static const edit = Icons.edit;
  static const newChat = Icons.message;
  static const backButtonArrow = Icons.arrow_back;
  static const arrowLeft = Icons.keyboard_arrow_left;
  static const arrowForward = Icons.arrow_forward;
  static const logout = Icons.exit_to_app;
  static const notifications = Icons.notifications;
  static const people = Icons.people;
  static const clear = Icons.clear;
  static final key = CustomIconData(A.icon('key'));
  static final attachment = CustomIconData(A.icon('attach'));
  static final attachmentCamera = CustomIconData(A.icon('camera'));
  static final attachmentImage = CustomIconData(A.icon('image'));
  static final attachmentFile = CustomIconData(A.icon('file'));
  static final authFacebook = CustomIconData(A.icon('facebook'));
  static final authGoogle = CustomIconData(A.icon('google'));
  static final authApple = CustomIconData(A.icon('apple'));
  static final feed = CustomIconData(A.icon('feed'));
  static final nearby = CustomIconData(A.icon('nearby'));
  static final add = CustomIconData(A.icon('plus'));
  static final messages = CustomIconData(A.icon('envelope'));
  static final myProfile = CustomIconData(A.icon('user'));
  static final feedLike = CustomIconData(A.icon('heart'));
  static final feedLikeOutline = CustomIconData(A.icon('heart_outline'));
  static final feedComment = CustomIconData(A.icon('speech-bubble'));
  static final feedShare = CustomIconData(A.icon('forward-arrow'));
}
